prompt("Enter 1 letter: ");

var letter = prompt("Enter the first number: ");

if (letter === A || letter === a || letter === E || letter === e || letter === I || letter === i || letter === O || letter === o || letter === U || letter === u) {
  alert("The letter you inputed is a vowel")
  
} else {
  alert("The value you inputed is not a vowel");
  
}